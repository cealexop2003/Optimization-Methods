function gamma = step_fixed(alpha)
% STEP_FIXED - Returns a constant step size (fixed learning rate).
%
% Input:
%   alpha : the constant step size (scalar, alpha > 0)
%
% Output:
%   gamma : the returned step size (here, always equal to alpha)

    gamma = alpha;

end
